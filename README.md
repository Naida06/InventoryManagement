# InventoryManagement

This repository contains a simple login page and a Selenium test.

## Running the Selenium test

1. Install dependencies:

```bash
pip install selenium webdriver-manager
```

2. Run the test script:

```bash
python3 tests/test_login.py
```

The test opens the `login.html` page, enters default credentials and verifies the success message.
